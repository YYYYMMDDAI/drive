# Claude Code ルール

## 回答形式ルール

### 1. 技術選定理由の明示

回答の文末に、使用した技術・ツール・ライブラリの選定理由を以下の形式で記載すること：

```
---
## 技術選定理由

### なぜ [技術名] を使ったか
- **理由**: [具体的な理由・メリット]
- **代替案**: [他の選択肢と、それを選ばなかった理由]
```

**例:**
```
---
## 技術選定理由

### なぜ Vite を使ったか
- **理由**: ビルド速度が高速で、ESモジュールネイティブ対応。PWAプラグインが充実
- **代替案**: Create React App（CRA）も可能だが、ビルド速度が遅く、PWA設定が複雑

### なぜ IndexedDB を使ったか
- **理由**: 大容量データの非同期保存が可能、オフライン対応に最適
- **代替案**: LocalStorageも可能だが、5MB制限があり同期的なため大量データに不向き
```

---

### 2. 初心者向け確認方法の明示

回答の文末（技術選定理由の下）に、初心者でも理解できる確認方法を以下の形式で記載すること：

```
---
## 初心者向け確認方法

### 動作確認手順
1. [具体的なコマンドまたは操作]
2. [期待される結果]
3. [うまくいかない場合の対処法]

### よくあるエラーと解決法
| エラー | 原因 | 解決方法 |
|--------|------|----------|
| [エラー内容] | [原因] | [解決方法] |

### 確認ポイント
- [ ] [チェック項目1]
- [ ] [チェック項目2]
```

**例:**
```
---
## 初心者向け確認方法

### 動作確認手順
1. ターミナルで `cd app` を実行してappディレクトリに移動
2. `npm install` を実行して依存関係をインストール（初回のみ）
3. `npm run dev` を実行して開発サーバーを起動
4. ブラウザで http://localhost:5173 を開く
5. 「Tool」という画面が表示されれば成功

### よくあるエラーと解決法
| エラー | 原因 | 解決方法 |
|--------|------|----------|
| command not found: npm | Node.jsがインストールされていない | https://nodejs.org からインストール |
| ENOENT: no such file | 間違ったディレクトリにいる | `cd app` でディレクトリを確認 |
| port 5173 already in use | 他のプロセスがポートを使用中 | `lsof -i :5173` で確認し終了するか、別ポートで起動 |

### 確認ポイント
- [ ] Node.js v18以上がインストールされているか (`node -v` で確認)
- [ ] appディレクトリ内にいるか (`pwd` で確認)
- [ ] node_modulesフォルダが存在するか (`ls node_modules` で確認)
- [ ] ブラウザでページが表示されるか
```

---

## 開発・デプロイフロー

### 毎回の開発フロー（忘れないこと）

```
┌─────────────────────────────────────────────────────────────┐
│                    開発・確認・デプロイフロー                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. ローカル開発                                             │
│     cd app && npm run dev                                   │
│     → http://localhost:5173 で確認                          │
│                                                             │
│  2. ビルド確認                                               │
│     npm run build                                           │
│     → エラーがないことを確認                                  │
│                                                             │
│  3. 本番プレビュー（ローカル）                                │
│     npm run preview                                         │
│     → http://localhost:4173 で本番相当の動作確認             │
│                                                             │
│  4. デプロイ（プレビュー版）                                  │
│     npm run deploy                                          │
│     → 一意のURLが発行される（リンク限定公開）                 │
│     → 例: https://app-xxxx-username.vercel.app              │
│                                                             │
│  5. デプロイ（本番版）※必要な場合のみ                        │
│     npm run deploy:prod                                     │
│     → 固定URLにデプロイ                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### コマンド早見表

| 目的 | コマンド | 結果 |
|------|----------|------|
| 開発サーバー起動 | `npm run dev` | http://localhost:5173 |
| ビルド | `npm run build` | dist/ に出力 |
| ビルド結果プレビュー | `npm run preview` | http://localhost:4173 |
| プレビューデプロイ | `npm run deploy` | 一意URL発行（限定公開） |
| 本番デプロイ | `npm run deploy:prod` | 固定URL更新 |

### Vercel初回セットアップ（1回だけ）

```bash
cd app
npx vercel login
# ブラウザが開くのでログイン（GitHub/GitLab/Email）
```

### デプロイURL仕様

- **プレビュー版**: `https://app-[ランダム文字列]-[username].vercel.app`
  - 毎回異なるURL
  - 検索エンジンに登録されない（robots.txt + noindex）
  - リンクを知っている人のみアクセス可能

- **本番版**: `https://[project-name].vercel.app`
  - 固定URL
  - `deploy:prod` で更新

### iPhoneでの確認フロー

```
1. npm run deploy でデプロイ
2. 表示されたURLをコピー
3. iPhoneのSafariでURLを開く
4. 共有 → ホーム画面に追加
5. ホーム画面からアプリとして起動
6. 更新時は再度 npm run deploy → Safariでアクセス
```

### 更新時の手順

```bash
# コード変更後
npm run build          # ビルド確認
npm run preview        # ローカルで本番相当の動作確認
npm run deploy         # プレビューデプロイ
# → 発行されたURLでiPhoneから確認
```

---

## 適用タイミング

- コード実装後の説明時
- ツール・ライブラリの導入時
- 設定ファイルの作成・変更時
- エラー修正後の説明時

軽微な修正や質問への単純な回答では省略可。
